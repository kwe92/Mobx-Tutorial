import styled, { createGlobalStyle } from "styled-components";

const AppGlobalTheme = createGlobalStyle`

  * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

 body{}

    li {
        line-style: none;
    }

`;

export { AppGlobalTheme }
